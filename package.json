{
  "scripts": {
    "build": "tsc --build ./Script/Source/tsconfig.json",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^17.0.8",
    "dotenv": "^11.0.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^17.0.8",
    "dotenv": "^11.0.0"
  }
}
